{
  "$schema": "https://openapi.vercel.sh/vercel.json",

  "// plano": "Configurado para o plano Hobby: sem cron interno (usar agendador externo) e maxDuration de 60s, que é seguro no gratuito.",
  "// regions": "pdx1 = Portland (AWS us-west-2), a mesma região do Supabase, para as consultas ao banco não cruzarem os EUA.",

  "regions": ["pdx1"],

  "functions": {
    "api/whatsapp/process.js":        { "maxDuration": 60 },
    "api/whatsapp/webhook.js":        { "maxDuration": 15 },
    "api/cron/followups.js":          { "maxDuration": 60 },
    "api/payments/kiwify-webhook.js": { "maxDuration": 30 }
  }
}
